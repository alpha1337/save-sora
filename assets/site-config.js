window.SORA_SAVE_POLL_CONFIG = {
  // Set this to the deployed anonymous poll backend origin when GitHub Pages
  // is serving the frontend, for example: "https://save-sora-poll.vercel.app/api"
  apiBase: "",
};
